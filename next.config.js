/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.ablula.tech',
      "malette.s3.ap-southeast-1.amazonaws.com",
    ],
  },
};

module.exports = nextConfig;
